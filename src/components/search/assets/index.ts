/**
 * Search Assets
 *
 * This directory contains icons, static protocol docs, and styles
 * for the /search feature. Add SVG icons, MDX protocol documents,
 * and feature-specific CSS modules here.
 *
 * Structure:
 *   assets/
 *   ├── icons/        — SVG icon components
 *   ├── protocols/    — MDX/JSON protocol documents
 *   └── styles/       — Feature-specific CSS modules
 */

export {};
